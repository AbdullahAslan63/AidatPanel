# Dönüm Noktası - 2026-05-06 15:09

## Son Commit Hash
`79680e571b47e303d39f0eb381170a1cbcbfe843`

## Yapılan Değişiklikler

| Dosya | Değişiklik | Açıklama |
|-------|-----------|----------|
| `mobile/lib/features/buildings/presentation/widgets/invite_confirm_dialogs.dart` | RichText → Text.rich | Cümle parçalı TextSpan kullanımı düzeltildi (2 dialog) |
| `mobile/lib/shared/widgets/settings_tab.dart` | Dialog tasarımı güncellendi | Çıkış dialog'u standart tasarıma getirildi, _DialogActionRow eklendi |

## Detaylar

### Aşama 1: L10n Öncesi Hata Düzeltme
- **OccupiedApartmentConfirmDialog (satır 40-56):** `RichText(text: TextSpan(...))` → `Text.rich(TextSpan(...))`
- **RevokeInviteCodeDialog (satır 97-111):** `RichText(text: TextSpan(...))` → `Text.rich(TextSpan(...))`
- **Amaç:** Cümle parçalama sorunu düzeltildi, tüm cümleler tek parça haline getirildi
- **Tasarım:** Kırmızı bold vurgu korundu

### Aşama 2: Dialog Tasarımı Standartlaştırma
- **Çıkış Dialog (settings_tab.dart):**
  - AlertDialog shape: `borderRadius: 16`
  - Title: Row with Icon (logout) + Text
  - actionsPadding: `EdgeInsets.fromLTRB`
  - Actions: _DialogActionRow widget'ı (Vazgeç + Çıkış Yap)
- **_DialogActionRow Widget:**
  - Yeni widget eklendi (invite_confirm_dialogs.dart ile aynı)
  - Vazgeç: OutlinedButton (borderRadius: 10)
  - Confirm: ElevatedButton (borderRadius: 10, dynamic color)
- **Tüm Dialog'lar Kontrol Edildi:**
  - invite_code_screen.dart → OccupiedApartmentConfirmDialog ✅
  - invite_code_screen.dart → RevokeInviteCodeDialog ✅
  - settings_tab.dart → Çıkış Dialog ✅
  - Hepsi aynı tasarıma sahip

## Standart Dialog Tasarımı (Gelecek İçin Referans)

```dart
AlertDialog(
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
  title: Row(
    children: [
      Icon(icon, color: color),
      const SizedBox(width: 8),
      const Text('Title'),
    ],
  ),
  content: Text('Content'),
  actionsPadding: const EdgeInsets.fromLTRB(
    AppSizes.spacingM,
    0,
    AppSizes.spacingM,
    AppSizes.spacingM,
  ),
  actions: [
    _DialogActionRow(
      confirmLabel: 'Confirm',
      confirmColor: AppColors.primary,
      onConfirm: () {},
    ),
  ],
)
```

## Aktif Dosyalar
- `mobile/lib/features/buildings/presentation/widgets/invite_confirm_dialogs.dart`
- `mobile/lib/shared/widgets/settings_tab.dart`

## Notlar
- Tüm proje tarandı, sadece invite_confirm_dialogs.dart'da cümle parçalama sorunu bulundu
- Tüm dialog'lar standartlaştırıldı
- L10n ekleme öncesi hazırlık tamamlandı
- Sonraki adım: L10n sistemini sıfırdan kurma
